
int num = 25;
var raiz = Math.Sqrt(num); // raiz cuadrada
Console.WriteLine(raiz);

float flot = 9.99f;
var redondeado = Math.Round(flot); // redondea un numero // 10

var maximo = Math.Max(10, 50); // retorna el mayor de dos numeros

var minimo = Math.Min(20, 30); // retorna el menor de dos numeros

